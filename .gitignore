# Python / uv
__pycache__/
*.pyc
.venv/
.uv/

# Local agent state (keep skills + settings.json tracked; ignore transient/local bits)
.claude/memory/
.claude/usage-data/
.claude/*.local.json

# Real fleet inventory (commit only the sanitized example)
fleet/fleet.yaml

# Generated experiment artifacts (the demo regenerates these; verdicts live in RESULTS_*.md)
experiments/*/raw.json
experiments/*/gate_evaluation.json
experiments/*/null.json
experiments/*/null_gate.json

# Editor / OS
.DS_Store
*.swp
