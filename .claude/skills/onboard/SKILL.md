---
name: onboard
description: Alias for atlas-setup - the guided project setup/reconfigure wizard. Use when the user says "/onboard", "onboard me", "get started", "set up this project", or "configure Atlas". Follow the procedure in .claude/skills/atlas-setup/SKILL.md.
---

# /onboard - alias for /atlas-setup

This is an alias. Open and follow **`.claude/skills/atlas-setup/SKILL.md`** - the guided wizard that
walks the user through naming, canonical state, ownership, agents, the optional fleet layer, memory,
the first experiment, and git/remote/attribution (confirming before anything destructive or
outward-facing). Re-runnable to change settings later, not just first-time.
